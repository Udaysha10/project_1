pipeline{
    agent any
    stages{
        stage("checkout"){
            steps{
                sh "echo "${BUILD_NUMBER}"
                sh "git --version"
                sh "whoami"
            }
        }
    }
}