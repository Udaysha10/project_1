pipeline{
    agent any
    stages{
        stage("checkout"){
            steps{
                echo "${BUILD_NUMBER}"
                sh "git --version"
                sh "whoami"
            }
        }
    }
}